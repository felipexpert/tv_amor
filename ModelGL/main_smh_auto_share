import subprocess

import pyautogui
from utils_smh.classes.config_smh import ConfigSmh
from utils_smh.classes.manual import Manual
from utils_smh.classes.manual_gl import ManualGL
from utils_smh.utils_load_manual_json import load_manual
from utils_smh.utils_manual_gui import chrome_work, run_works
from utils_smh.utils_manual_to_manual_gl import manual_to_manual_gl
from utils_smh.utils_paths_config import load_config


def main():
    debug = False
    config_smh:ConfigSmh = load_config()
    if debug: print("config_smh", config_smh)
    manual:Manual = load_manual()
    manual_gl:ManualGL = manual_to_manual_gl(manual)
    
    # print(manual_gl)
    
    # run_works(manual)
    
    chrome_work("")
    # pyautogui.sleep(10)
    # chrome_work("Profile 15")
    # pyautogui.sleep(10)
    # chrome_work("Profile 10")
    
if __name__ == "__main__":
    main()
